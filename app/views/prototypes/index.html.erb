<main class="main">
  <div class="inner">
      <%# ログインしているときは以下を表示する %>
     <% if user_signed_in?%> 
       <div class="greeting">
         こんにちは、
        <%= link_to "#{current_user.name}さん", user_path(current_user), class: :greeting__link%>
        <%#="こんにちは、#{current_user.name}さん" %>
        <%# else %>
        <%# link_to "ログアウト", destroy_user_session_path, class: :greeting__link %>
        <%# link_to "ログアウト", user_registration_path, class: :greeting__link%>
       </div> 
     <% end %>
      <%# // ログインしているときは上記を表示する %>
    <div class="card__wrapper">
      <%# 投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
      <%= render partial: 'prototype', collection: @prototypes %>
      <%#= render partial: 'prototypes/prototype', locals: @user.prototypes %>
    </div>
  </div>
</main>
